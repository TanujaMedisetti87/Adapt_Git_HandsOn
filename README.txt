1. Every individual has to fork this repository
2. All the three teams should write their name in the header and every individual of the team has to upload their photo in 
   place of the default image
3. After the image is clicked a default resume is opened. Add the URL of your deployed Resume( AWS or OpenShift) in place of the defaut URL
